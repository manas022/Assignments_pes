//TOPIC - Assignment of variables:

// let name;
// console.log(name);// the command terminal will show undefined as there is no value assingned to the variable.

//Topic - Object:

let person= {
    name: 'Manas',
    age : 25
};

// console.log(person);
//We can also use the dot notation and the bracket notation. ('.', '[]')
//such as:

//This is the bracket notation. note that you have to mention the key with the object in inverted commas.("")
person['name'] = 'maddy';
console.log(person);

//And this is the (".") dot notation. 
person.name = "millionare";
console.log(person);